info:
  name: Delete Note
  type: http
  seq: 7

http:
  method: DELETE
  url: http://localhost:8080/notes/2
  auth:
    type: bearer
    token: "{{jwt_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
