info:
  name: Generate Title (AI)
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8080/ai/generate-title
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
