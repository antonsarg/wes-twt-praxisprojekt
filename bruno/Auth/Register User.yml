info:
  name: Register User
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/register"
  body:
    type: json
    data: |-
      {
        "email": "anton@example.com",
        "password": "securepassword123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
