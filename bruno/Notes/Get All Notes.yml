info:
  name: Get All Notes
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/notes"
  auth:
    type: bearer
    token: "{{jwt_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
