info:
  name: Delete Note
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{baseUrl}}/notes/2"
  auth:
    type: bearer
    token: "{{jwt_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
